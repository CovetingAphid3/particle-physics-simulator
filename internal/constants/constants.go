package constants
// Physics Constants

// Gravitational constant (G) in m³/(kg·s²)
const GravitationalConstant = 6.67430e-11 // m³/(kg·s²)

// Coulomb's constant (k_e) in N·m²/C²
const CoulombsConstant = 8.9875e9 // N·m²/C²

// Permittivity of free space (ε₀) in C²/(N·m²)
const PermittivityOfFreeSpace = 8.854e-12 // C²/(N·m²)

// Electric charge of the electron (e) in Coulombs
const ElectronCharge = 1.602e-19 // C

// SI unit conversion constants
const MetersToPixels = 100.0 // Conversion from meters to pixels for rendering
const SecondsPerFrame = 1.0 / 120.0 // Simulation frame rate (120 FPS)

// Particle Constants

// Default mass and radius for particles (for simplicity)
const DefaultMass = 1.0 // in kg
const DefaultRadius = 10.0 // in pixels

